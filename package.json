{
  "name": "argus",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source agentic OSINT intelligence platform",
  "author": "Stratir <https://github.com/stratir>",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter ui dev\"",
    "build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter ui build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}